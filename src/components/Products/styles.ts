import { Box, styled } from "@mui/material";

export const InfinityLoadingFooter = styled(Box)({
  background: "rgba(0 0 0 / 0.1)",
  height: "80px",
  width: "100%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});
